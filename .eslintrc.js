module.exports = {
  root: true,
  extends: '@react-native',

  // TODO:
  // - Install eslint.
  // - Fix this eslint config (it can't find @react-native).
  // - Enable import-sorting.
  // - Add @tanstack/eslint-plugin-query.
};
